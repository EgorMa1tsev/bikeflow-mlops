BIKEFLOW_LOG_LEVEL=INFO
BIKEFLOW_MODEL_PATH=models/model.joblib
BIKEFLOW_DB_PATH=data/predictions.db
BIKEFLOW_MONITORING_DIR=data/monitoring
# Serve the model from the MLflow registry; required for retraining.
# BIKEFLOW_MODEL_URI=models:/bikeflow-demand@champion
BIKEFLOW_RETRAINING_DIR=data/retraining
# Where the Streamlit interface looks for the API.
BIKEFLOW_API_URL=http://127.0.0.1:8000
